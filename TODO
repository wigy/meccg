TODO
====

FIXES
-----

FEATS
-----

EPICS
-----

Tests for game rules (step by step go through again each CoE rule, build status matrix)
Company vs Company and Influence Attempt vs oppoonent
The Dragons Rules
Dark Minions Rules
The Lidless eye Rules
Against the shadow Rules
The White Hand Rules
The Balrog Rules
Deck Validation
Tournaments

UNSORTED
--------

Uploading Gccg deck format.

Managing AI requests in a different copy of the server repository, but on the same server. After fix is pushed, tell server using master key and 'server' user to log in and to restart everything. Server must notify everyone, pull the code and then restart all processes and reload web client code.

Pseudo AI: You will get list of actions as an opponent of AI and select it in every step. Displayed in both debug or visual UI.

One single full game played with 2 Cypress robots running in parallel. Need to have --seed <num> support for game server and ability to Cypress clients to set it. Maybe we need to check result of every roll so that we can see if random system diverges due to additional die roll for example. Or vs. pseudo ai.
