{
  "name": "@meccg/text-client",
  "version": "0.138.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "start": "npx tsx src/console-client.ts",
    "dev": "nodemon --watch src --watch ../shared/src --ext ts,json --exec 'npx tsx src/console-client.ts'",
    "dev:debug": "DEBUG=1 nodemon --watch src --watch ../shared/src --ext ts,json --exec 'npx tsx src/console-client.ts'"
  },
  "dependencies": {
    "@meccg/shared": "*",
    "@meccg/sim": "*",
    "ws": "^8.19.0"
  }
}
