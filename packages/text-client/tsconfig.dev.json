{
  "//": "tsx config for clients run straight from source (bin/observe): maps the workspace packages onto their SOURCE instead of their built dist. The observer explains what the agent code says right now, so resolving a stale dist would have it explain the last build instead of the working tree.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@meccg/shared": ["../shared/src/index.ts"],
      "@meccg/shared/*": ["../shared/src/*"],
      "@meccg/sim": ["../sim/src/index.ts"],
      "@meccg/sim/*": ["../sim/src/*"],
      "@meccg/game-server": ["../game-server/src/index.ts"],
      "@meccg/game-server/*": ["../game-server/src/*"]
    }
  }
}
