{
  "name": "@meccg/client-web",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/browser/app.ts --bundle --outfile=public/bundle.js --format=iife --platform=browser",
    "start": "npm run build && npx tsx src/server/serve.ts",
    "dev": "npm run build && (esbuild src/browser/app.ts --bundle --outfile=public/bundle.js --format=iife --platform=browser --watch=forever & npx tsx src/server/serve.ts --dev)"
  },
  "dependencies": {
    "@meccg/shared": "*",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
