{
  "name": "@meccg/shared",
  "version": "0.8.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "test:nightly": "vitest run --config vitest.nightly.config.ts"
  },
  "dependencies": {
    "mathjs": "^15.1.1"
  }
}
