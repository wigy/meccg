/**
 * @module 11-items-rings.test
 *
 * Tests for CoE Rules Section 6: Items and Rings.
 *
 * Rule references from docs/coe-rules.md.
 */

import { describe, test } from 'vitest';

describe('6 Items', () => {
  test.todo('[6] items played during site phase by untapped character at untapped site');
  test.todo('[6] items classified by type: minor, major, greater, gold ring');
  test.todo('[6] characters bearing items receive bonuses and abilities');
  test.todo('[6] when character leaves play, all items it bears are discarded');
  test.todo('[6] movement restrictions on items always apply regardless of alignment');
  test.todo('[6] only one weapon may be used at a time');
  test.todo('[6] item "use" is not the same as it being in play');
});

describe('6 Rings', () => {
  test.todo('[6] gold ring: may be tested to determine type of ring special item');
  test.todo('[6] ring test: roll 2d6, result determines ring type playable');
  test.todo('[6] The One Ring: special rules for playing and corruption');
});

describe('6 Hoard items', () => {
  test.todo('[6] hoard items can only be played at sites with hoard automatic-attacks');
  test.todo('[6] hoard minor items are non-unique');
});
