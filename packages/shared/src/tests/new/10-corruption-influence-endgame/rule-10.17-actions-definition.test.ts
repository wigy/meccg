/**
 * @module rule-10.17-actions-definition
 *
 * CoE Rules — Section 10: Corruption, Influence, Actions/Timing & Ending the Game
 * Rule 10.17: Actions Definition
 *
 * Source: docs/coe-rules.txt
 */

/*
 * RULING:
 *
 * An action is a one-time activity that changes the board state when it is performed. A player declaring that they are initiating an allowance that involves one or more actions is commonly referred to as "taking an action."
 * Actions cannot be performed without an allowance or requirement to do so.
 * An allowance on a card can only be initiated by the player of that card (unless the card states otherwise, or is written as allowing any entity of a specified type to initiate the allowance).
 */

import { describe, test } from 'vitest';

describe('Rule 10.17 — Actions Definition', () => {
  test.todo('Action = one-time board state change; taking action = initiating an allowance; cannot perform without allowance');
});
