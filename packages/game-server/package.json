{
  "name": "@meccg/game-server",
  "version": "0.20.4",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "start": "npx tsx src/ws/server.ts",
    "dev": "nodemon --watch src --watch ../shared/src --ext ts,json --exec 'npx tsx src/ws/server.ts --dev'",
    "dev:debug": "DEBUG=1 nodemon --watch src --watch ../shared/src --ext ts,json --exec 'npx tsx src/ws/server.ts --dev'",
    "dev:verbose": "VERBOSE=1 nodemon --watch src --watch ../shared/src --ext ts,json --exec 'npx tsx src/ws/server.ts --dev'"
  },
  "dependencies": {
    "@meccg/shared": "*",
    "ws": "^8.19.0"
  }
}
