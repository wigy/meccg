{
  "name": "@meccg/server",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "start": "npx tsx src/ws/server.ts",
    "dev": "nodemon --watch src --watch ../shared/src --ext ts,json --exec 'npx tsx src/ws/server.ts'",
    "dev:debug": "DEBUG=1 nodemon --watch src --watch ../shared/src --ext ts,json --exec 'npx tsx src/ws/server.ts'"
  },
  "dependencies": {
    "@meccg/shared": "*",
    "ws": "^8.19.0"
  }
}
