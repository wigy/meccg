{
  "name": "@meccg/lobby-server",
  "version": "0.14.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "start": "npx tsx src/server.ts",
    "dev": "nodemon --watch src --watch ../shared/src --watch ../web-client/public --ext ts,json,html,css,js --ignore ../web-client/public/bundle.js --exec 'npm run build -w @meccg/web-client && npx tsx src/server.ts --dev'"
  },
  "dependencies": {
    "@meccg/shared": "*",
    "ws": "^8.19.0"
  }
}
