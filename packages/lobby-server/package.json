{
  "name": "@meccg/lobby-server",
  "version": "0.20.2",
  "private": true,
  "scripts": {
    "build": "tsc && npm run build:browser",
    "build:browser": "esbuild src/browser/app.ts --bundle --outfile=public/bundle.js --format=iife --platform=browser",
    "check": "tsc --noEmit",
    "start": "npm run build:browser && npx tsx src/server.ts",
    "dev": "nodemon --watch src --watch ../shared/src --watch public --ext ts,json,html,css,js --ignore public/bundle.js --exec 'npm run build:browser && npx tsx src/server.ts --dev'"
  },
  "dependencies": {
    "@meccg/shared": "*",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
