{
  "name": "@meccg/lobby-server",
  "version": "0.10.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "start": "npx tsx src/server.ts",
    "dev": "nodemon --watch src --watch ../shared/src --watch ../web-client/public --ext ts,json,html,css,js --exec 'npx tsx src/server.ts --dev'"
  },
  "dependencies": {
    "@meccg/shared": "*",
    "ws": "^8.19.0"
  }
}
