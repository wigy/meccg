/**
 * @module game-bundle
 *
 * esbuild entry alias: bundles the game view entry (`game-entry.ts`) into
 * `public/game-bundle.js`. See `bundle.ts` for why the alias exists.
 */
import './game-entry.js';
