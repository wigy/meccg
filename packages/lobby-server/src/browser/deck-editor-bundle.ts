/**
 * @module deck-editor-bundle
 *
 * esbuild entry alias: bundles the deck editor entry (`deck-editor-entry.ts`)
 * into `public/deck-editor-bundle.js`. See `bundle.ts` for why the alias
 * exists.
 */
import './deck-editor-entry.js';
